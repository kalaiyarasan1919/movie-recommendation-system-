services:
  - type: web
    name: movie-recommendation-system
    env: python
    buildCommand: pip install -r requirements-basic.txt
    startCommand: gunicorn app-simple:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16 